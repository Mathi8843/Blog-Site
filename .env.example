# Optional: GitHub Personal Access Token for accurate commit count on the homepage at build time.
# Classic PAT: scope `read:user` is enough for GraphQL contribution totals on your own login.
# Fine-grained: read access to user profile / metadata as required by GitHub.
# Set in Vercel: Project → Settings → Environment Variables → GITHUB_TOKEN
GITHUB_TOKEN=
